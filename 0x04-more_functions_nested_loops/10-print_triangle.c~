#include "main.h"


void print_triangle(int size)
{
	int c, l;

	if (size <= 0)
		_putchar('\n');

	for (c = 1; c <= size; c++)
	{
		for (l = 2; l <= size; l++)
		{
			_putchar(' ');
		}

		for (l = 1; l <= c; l++)
		{
			_putchar('#');
		}
		_putchar('\n');
	}
 }
